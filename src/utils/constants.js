import {
    heightPercentageToDP as hp,
    widthPercentageToDP as wp,
} from 'react-native-responsive-screen';

export const defaultStyles = {
    primaryColor: "#780832",
    secondaryColor: "#FFFFFF",
    fontColor: "#606060",
    fontColorLight: "#8e8a86",
    widthPercentage: wp,
    heightPercentage: hp
}

export const apiUrl = '';